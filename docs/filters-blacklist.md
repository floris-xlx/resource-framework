### Filter blacklist

- The special `actions` column is marked non-filterable and excluded from:
  - URL filter parsing
  - Advanced filter field list

- To mark a column as non-filterable, set `meta.filterable = false` on the column spec.

