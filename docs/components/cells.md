# Cells

Exported via `resource-framework/components`:

- `ScopeCell` — renders country flag + label for formation scopes
- `AssigneesCell` — displays small avatars/initials for assignees

```tsx
import { ScopeCell, AssigneesCell } from "resource-framework/components";
```


